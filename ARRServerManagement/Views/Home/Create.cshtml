@model ARRServerManagement.Models.CreateSession

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Session</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="maxLeaseTime" class="control-label"></label>
                <input asp-for="maxLeaseTime" class="form-control" />
                <span asp-validation-for="maxLeaseTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="size" class="control-label"></label>
                <select asp-for="size" asp-items="@Html.GetEnumSelectList(typeof(SessionSize))">
                </select>
                <span asp-validation-for="size" class="text-danger"></span>
            </div>
            @*<div class="form-group">
                <label asp-for="models" class="control-label"></label>
                <input asp-for="models" class="form-control" />
                <span asp-validation-for="models" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
